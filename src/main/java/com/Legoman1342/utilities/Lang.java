package com.Legoman1342.utilities;

import java.util.Locale;

public class Lang {
	public static String asId(String name) { //Makes a given string lowercase, formatting it as an ID
		return name.toLowerCase(Locale.ROOT);
	}
}
